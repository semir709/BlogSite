<% if(typeof errors != 'undefined') { %>
    <% errors.forEach(error => { %>
        <%= error.msg; %>
    <% }); %> 
<% } %>

<% if(succes_msg != '') { %>
    <%= succes_msg %>
<% } %>

<% if(typeof message != 'undefined') { %>
    
    <%= message %>
     
<% } %>

